

const NewAccountValidation = (props) => {
    console.log(props.handleThisShit)
    return(
        <button onClick={props.handleThisShit}>click</button>
    )   
} 

export default NewAccountValidation